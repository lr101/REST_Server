**********************************
Database (sensors):

Table -> id:
--------------------
1. sensorID --- primary key
2. sensorNick
3. sensorTypeID

Table -> types:
--------------------
1. sensorTypeID --- primary key
3. sensorType

Table -> {sensorID}
--------------------
1. date
2. value
3. rowID --- primary key


***********************************
REST-API:
host address: {host-IP}:{host-port}/sensors/

Table -> id:
---------------------
GET /id/     					                //returns the complete Table -> id (and types.sensorType)
GET /id/{sensorID}				                //returns row with of the given sensorID
GET /id/{sensorID}/sensorTypeID			        //returns sensorTypeID of specific sensorID
GET /id/{sensorID}/sensorType			        //returns types.sensorType of specific sensorID
GET /id/{sensorID}/sensorNick			        //returns sensorType of specific sensorID
PUT /id/{sensorID}                              //updates sensorNick or/and sensorTypeID json format: {"sensorTypeID": "v1", "sensorNick" : "v2"}
POST /id/					                    //add new Sensor to Table -> id and create Table -> {sensorID} body: {"sensorID":"{sensorID}", "sensorNick":"{sensorNick}", "sensorType":"{sensorType}"}
DELETE /id/{sensorID}/				            //delete Table -> {sensorID} and entry in Table -> id *restricted access

Table -> {sensorID}
----------------------
GET /{sensorID}/				                //returns the complete Table -> {sensorID}
GET /{sensorID}?date1={date1}&date2={date2}	    //returns the table from date1 until date2
POST /{sensorID}/				                //adds a new table entry with body as JSON: {"date":"{date1}", "value":"{value1}"}
DELETE /{sensorID}?date1={date1}&date2={date2}	//delete entries from date1 to date2. If no dates are given, all entries are deleted




